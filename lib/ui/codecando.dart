import 'package:flutter/material.dart';

class CodeCanDoView extends StatelessWidget {
  const CodeCanDoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('data'),
    );
  }
}
